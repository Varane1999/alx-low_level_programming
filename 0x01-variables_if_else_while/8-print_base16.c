#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0
 */

int main(void)
{
	char w;

	int d;

	w = 'a';
	d = 0;
	while
		(d < 10) {
			putchar(d + '0');
			d++;
		}
	while
		(w <= 'f') {
			putchar(w);
			w++;
		}
	putchar('\n');
	return (0);
}
