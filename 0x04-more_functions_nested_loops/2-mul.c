#include "main.h"

/**
 * mul - functions checks for a digit (0 through).
 * @a: a - Variable 1
 * @b: b - Variable 2
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
